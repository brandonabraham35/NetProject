# Contributing

1. Fork the repository.
2. Create a feature branch: `git checkout -b feature/new-provider`.
3. Ensure you do not hardcode secrets or break frontend logic. Map all output to `Normalizer`.
4. Submit a Pull Request.

All commits should pass the CI/CD pipeline defined in `.github/workflows/main.yml`.
